import React from "react";

const ButtonContainer = ({ children }) => {
  return <div className="flex items-center text-center mx-auto my-4 px-auto justify-center">{children}</div>;
};

export default ButtonContainer;
